# Task Manager API

This project provides a simple FastAPI-based Task Manager with CRUD operations on tasks.

## Setup

Install requirements:

```bash
pip install -r requirements.txt
```

Run tests:

```bash
pytest -q
```
